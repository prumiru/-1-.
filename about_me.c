#include <stdio.h>

int main() {
    int years = 15;

    printf("[마그라테아] ❤❤ 프로젝트 멤버 소개 ❤❤\n");
    printf("=====================================================================\n");
    printf("이름 | 아서 덴트     성별 | 남\n");
    printf("전공 | 기계 공학     경력 | ");

    // 이진수 출력 (4바이트, 32비트)
    for (int i = 31; i >= 0; i--) {
        printf("%d", (years >> i) & 1); // 비트 연산으로 이진수 출력
        if (i % 8 == 0 && i != 0) {
            printf(" "); // 8비트 공백 추가
        }
    }

    printf("(15)년\n"); // 4바이트 정보를 추가한다.
    printf("역할 | 아키텍트      기술 | C/C++, 자바, 스프링, 파이썬, 머신러닝/딥러닝\n");
    printf("---------------------------------------------------------------------\n");
    printf("소개 | 실리콘 밸리에서 SNS, 핀테크, HR, 미디어 등의 다양한 프로젝트에 참여하며 경력을 쌓았습니다.\n");
    printf("      성공과 실패를 겪으며 얻게 된 깨달음을 여러분과 나누고 싶습니다.\n");
    printf("=====================================================================\n");

    years = 2; // 경력 업데이트
    printf("이름 | 김영진   성별 | 남\n");
    printf("전공 | 컴퓨터공학   경력 | ");

    // 이진수 출력 (4바이트, 32비트)
    for (int i = 31; i >= 0; i--) {
        printf("%d", (years >> i) & 1); // 비트 연산으로 이진수 출력
        if (i % 8 == 0 && i != 0) {
            printf(" "); // 8비트 공백 추가
        }
    }

    printf("(2)년\n");
    printf("역할 | 개발자  기술 | C#, 파이썬, 자바스크립트, 리액트, 코틀린 \n");
    printf("---------------------------------------------------------------------\n");
    printf("소개 | 최근에 MAUI 프레임워크로 진행한 개인 자산 관리 프로젝트에서 소프트웨어 개발에 엄청난 매력을 느꼈습니다.\n");
    printf("      마그라테아를 성공적으로 개발하는 것은 저의 버킷 리스트 중 하나가 되었습니다.\n");
    printf("=====================================================================\n");

    years = 1; // 경력 업데이트
    printf("이름 | 임원철   성별 | 남\n");
    printf("전공 | 전자 공학   경력 | ");

    // 이진수 출력 (4바이트, 32비트)
    for (int i = 31; i >= 0; i--) {
        printf("%d", (years >> i) & 1); // 비트 연산으로 이진수 출력
        if (i % 8 == 0 && i != 0) {
            printf(" "); // 8비트 공백 추가
        }
    }

    printf("(1)년\n");
    printf("역할 | DBA  기술 | 자바, PHP, Mongo DB, MS SQL, MySQL \n");
    printf("---------------------------------------------------------------------\n");
    printf("소개 | 애플리케이션은 결국 데이터를 다루는 방식을 구현한 것이라고 생각합니다.\n");
    printf("=====================================================================\n");

    years = 1; // 경력 업데이트
    printf("이름 | 유고은 성별 | 여\n");
    printf("전공 | 경영 정보  경력 | ");

    // 이진수 출력 (4바이트, 32비트)
    for (int i = 31; i >= 0; i--) {
        printf("%d", (years >> i) & 1); // 비트 연산으로 이진수 출력
        if (i % 8 == 0 && i != 0) {
            printf(" "); // 8비트 공백 추가
        }
    }

    printf("(1)년\n");
    printf("역할 | 클라우드 기술 | 자바, PowerShell, Azure, AWS, GCP \n");
    printf("---------------------------------------------------------------------\n");
    printf("소개 | 김영진님과 함께 프로젝트를 진행했으며 클라우드 아키텍처 구현을 담당했습니다.\n");
    printf("=====================================================================\n");

    years = 0; // 경력 업데이트
    printf("이름 | 서혜경  성별 | 여\n");
    printf("전공 | 정치외교 경력 | ");

    // 이진수 출력 (4바이트, 32비트)
    for (int i = 31; i >= 0; i--) {
        printf("%d", (years >> i) & 1); // 비트 연산으로 이진수 출력
        if (i % 8 == 0 && i != 0) {
            printf(" "); // 8비트 공백 추가
        }
    }

    printf("(0)년\n");
    printf("역할 | 개발자 기술 | 파이썬, 스위프트, 코틀린, Node.js, 피그마 \n");
    printf("---------------------------------------------------------------------\n");
    printf("소개 | 기업의 사활이 걸린 실전 프로젝트는 제 인생의 첫 경험입니다.\n");
    printf("=====================================================================\n");

    return 0;
}
